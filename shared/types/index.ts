export interface name {
  en: string;
  ru: string;
}

export interface colors {
  light: string;
  dark: string;
}

export interface response {
  sign: string;
  language: string;
  period: string;
  horoscope: string;
}
